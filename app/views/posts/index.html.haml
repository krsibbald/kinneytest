.row
  .col-lg-12
    %h1 Listing posts

    %table.table
      %tr
        %th Title
        %th User
        %th
        %th
        %th

      - @posts.each do |post|
        %tr
          %td= post.title
          %td= post.user.name
          %td= link_to 'Show', post

    %br

    = link_to 'New Post', new_post_path
